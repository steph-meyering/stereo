# Temporary credentials for local development
# For production, use proper Rails encrypted credentials

secret_key_base: <%= SecureRandom.hex(64) %>

# AWS credentials (not needed for local development with disk storage)
# aws:
#   access_key_id: YOUR_ACCESS_KEY
#   secret_access_key: YOUR_SECRET_KEY
#   region: us-west-1
#   dev:
#     bucket: your-dev-bucket
#   prod:
#     bucket: your-prod-bucket

