<% if logged_in? %>
    <div id="bootstrap-current-user">
        <script type="text/javascript">
        window.currentUser = <%= render(
            partial: "api/users/user",
            formats: :json,
            locals: { user: current_user }
            ).html_safe %>
        </script>
    </div>
    
<% end %>

<main id="root">
  React is not working
</main>